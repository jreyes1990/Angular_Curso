const name: string = 'Strider';
let hpPoints: number = 95;
const isAlive: boolean = true;
let hpPoinstFull: number | string | boolean = '';

hpPoinstFull = true;

console.table({
  name, hpPoints, isAlive, hpPoinstFull
});

export {};